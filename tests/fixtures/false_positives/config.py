# False-positive secret examples - should NOT be flagged

# Placeholder / example values
api_key = "YOUR_API_KEY_HERE"
password = "changeme"
token = "example-token"

# Short values that don't look like real secrets
key = "abc"
